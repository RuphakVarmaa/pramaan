# ASSETS.md — asset manifest

Every third-party asset in this repo, with license and source. Anything not listed here is original work in this repo. If you add an asset, add a row before you commit it.

## Fonts

_(pending: swarm S6 — fonts land with the React console. Expected: self-hosted woff2 files under `web/public/fonts/`, referenced by the evidence-pack template with relative paths. Each font needs: name, file, license, and source URL below before it ships.)_

| Asset | Files | License | Source |
|---|---|---|---|
| _none yet — S6 to populate_ | — | — | — |

## Diagrams and graphics

All diagrams in this repo (the architecture triangle in README.md and ARCHITECTURE.md, the flow diagrams, the evidence-pack dossier design in `templates/`) are ASCII/in-repo originals authored for Pramaan — no stock imagery, no external assets. `metrics/chart.svg` (S5) is generated by the batch runner, not imported.

## Icons

None shipped. If the console (S6) introduces an icon set, it must be listed here with its license before merge.

## Fonts used by the evidence pack

The dossier renders serif-forward with self-hosted fonts from `web/public/fonts/` (same set as above, tracked under the S6 pending section until they land). No external font CDN is fetched — the pack is self-contained by design and must stay that way.
